{
  "compilerOptions": {
    "target": "ES6", // La versión de JavaScript a la que se compila.
    "module": "NodeNext", // El sistema de módulos que usa Node.js.
    "outDir": "./dist", // Directorio de salida para los archivos compilados.
    "strict": true, // Activa todas las comprobaciones estrictas de TypeScript.
    "moduleResolution": "NodeNext",// Permite que TypeScript resuelva módulos al estilo de Node.js buscando en node_modules.
  },
  "include": ["src/**/*.ts"], // Archivos a incluir en la compilación.
  "exclude": ["node_modules"] // Archivos a excluir.
}