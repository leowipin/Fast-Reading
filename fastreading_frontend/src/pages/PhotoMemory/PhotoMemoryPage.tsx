import styles from './PhotoMemory.module.css'

const PhotoMemoryPage = () =>{
    return <div className={styles.photoMemoryContainer}> ESTO ES CONTENIDO PREMIUM </div>
}

export default PhotoMemoryPage;
